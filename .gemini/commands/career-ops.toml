description = "career-ops — company-pipeline command menu"

prompt = """
Show the career-ops company-pipeline commands (this fork), then act on any argument.

Pipeline (LLM does the breadth; you only decide):
  /career-ops-triage-companies   Stage 2 — world-knowledge prerank of the queue (no web)
  /career-ops-research-companies Stage 3 — web-research the top of the queue → reports + scores
  /career-ops-find-companies     Discover NEW companies by criteria (config/company_criteria.yml)

Then YOU decide in the web console:  `npm run dashboard:decisions` → http://localhost:4174
After deciding:  `node generate-watchlist.mjs` then `node scan.mjs` (pull current postings).

Helpers:  node llm-triage.mjs --stats | --queue N   ·   node decide.mjs --list

Typical first run: `/career-ops-triage-companies 100` then `/career-ops-research-companies 20`.

Argument: {{args}}
"""
